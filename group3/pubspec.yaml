# pubspec.yaml

name: group3
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^0.13.6
  flutter_secure_storage: ^8.0.0
  jwt_decoder: ^2.0.1
  image_picker: ^1.0.7           # ✅ 이미지 선택용
  path: ^1.8.3                   # ✅ 파일 경로 처리용
  http_parser: ^4.0.2            # ✅ multipart contentType 설정용
  video_player: ^2.7.2           # ✅ 동영상 재생용
  font_awesome_flutter: ^10.4.0  # ✅ FontAwesome 아이콘

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
